# SonarCloud Automatic Analysis reads this file instead of sonar-project.properties
# (only a limited set of properties is supported here).
# Exclude binary/non-source assets to avoid file encoding warnings.
sonar.exclusions=app/static/**
